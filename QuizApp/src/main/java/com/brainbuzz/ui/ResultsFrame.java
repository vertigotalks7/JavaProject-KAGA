package com.brainbuzz.ui;

import javax.swing.*;
import java.awt.*;

/**
 * ResultsFrame - Displays score + explanations
 */
public class ResultsFrame extends JFrame {
    public ResultsFrame() {
        setTitle("Results");
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setSize(600, 400);
        setLocationRelativeTo(null);
        // Add results UI components here
    }
}
