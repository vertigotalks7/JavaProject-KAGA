package com.brainbuzz.ui;

import javax.swing.*;
import java.awt.*;

/**
 * AdminPanelFrame - Admin dashboard: manage Qs, users, tickets
 */
public class AdminPanelFrame extends JFrame {
    public AdminPanelFrame() {
        setTitle("Admin Panel");
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setSize(600, 400);
        setLocationRelativeTo(null);
        // Add admin panel UI components here
    }
}
