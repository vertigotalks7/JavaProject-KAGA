package com.brainbuzz.ui;

import javax.swing.*;
import java.awt.*;

/**
 * HistoryFrame - Shows past quiz attempts
 */
public class HistoryFrame extends JFrame {
    public HistoryFrame() {
        setTitle("History");
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setSize(600, 400);
        setLocationRelativeTo(null);
        // Add history UI components here
    }
}
